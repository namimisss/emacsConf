;; must first order
(require 'pluginInit)
;; must second order
(require 'emacsBasicConf)
;;(add-to-list 'load-path (expand-file-name "config/basicBehaiveConf" user-emacs-directory))
;; (add-to-list 'load-path (expand-file-name "config/windowConfMng" user-emacs-directory))
;;(add-to-list 'load-path "~/.emacs.d/config/commonPluginConf")
;;(add-to-list 'load-path (expand-file-name "config/programmingConf" user-emacs-directory))

;; (require 'commonPluginConfMng)
;;(require 'basicBehaiveConf)
;;(require 'programmingMng)
;;(require 'windowConfMng)

;; programming conf
(provide 'programmingMng)

(provide 'pluginMng)
